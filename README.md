# Configuring the project #

1. Run command "pip install -r requirements.txt" in console
2. Create PostgreSQL database.
3. Create ".env" file and fill it like ".env.example"
4. Run command "flask --app app init-db" in console
5. Run command "flask --app app run" to run application (add "--debug" to run in debug mode)